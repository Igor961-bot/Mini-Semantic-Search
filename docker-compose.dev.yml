services:
  app:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      ASI_CHROMA_PATH: /app/data/chroma
      ASI_LOG_PATH: /app/logs/app.log
